fastapi>=0.110
uvicorn[standard]>=0.29
EbookLib>=0.18
beautifulsoup4>=4.12
python-multipart>=0.0.9

# Giọng đọc offline (Piper). Bản >=1.3 (dự án OHF-Voice/piper1-gpl) có sẵn wheel
# cho Windows/macOS/Linux, không cần piper-phonemize nên cài dễ hơn hẳn.
# Nếu vẫn lỗi, xem README phần "Cách 2: dùng binary piper".
piper-tts>=1.3

# Đọc PDF (thuần Python, chạy tốt trên ARM)
pypdf>=4.0
